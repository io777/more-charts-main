stages:
    - dev

variables:
    DOCKER_HOST: tcp://docker:2376

dev:
    stage: dev
    only:
        - branches
    image: docker/compose:latest
    services:
        - docker:dind
    before_script:
        - apk add --no-cache make
    script:
        - make init
    tags:
        - runner_1